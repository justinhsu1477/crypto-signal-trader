# Cloud Architecture — hook-fi.com

## 網路架構圖

```
                        ┌─────────────────────────────────────┐
                        │          用戶 (瀏覽器)                │
                        └──────────────┬──────────────────────┘
                                       │ HTTPS (port 443)
                                       ▼
                        ┌─────────────────────────────────────┐
                        │        Cloudflare CDN / SSL          │
                        │                                      │
                        │  ✅ DDoS 防護                        │
                        │  ✅ SSL 終結 (用戶↔Cloudflare 加密)  │
                        │  ✅ 快取靜態資源                      │
                        │                                      │
                        │  DNS: hook-fi.com → Cloudflare Proxy │
                        └──────────────┬──────────────────────┘
                                       │ HTTP (port 80)  ← Flexible 模式
                                       ▼
┌──────────────────────────────────────────────────────────────────────┐
│                 DigitalOcean Droplet (Singapore)                      │
│                 Ubuntu 24.04 / 2GB RAM                                │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                    Docker Network (內部)                        │  │
│  │                                                                │  │
│  │  ┌──────────────────────┐                                      │  │
│  │  │   Caddy Container     │  ← 0.0.0.0:80  (對外)              │  │
│  │  │   caddy:2-alpine      │  ← 0.0.0.0:443 (對外，未使用)      │  │
│  │  │                       │                                     │  │
│  │  │   /api/*  ──────────────→ trading-api:8080                  │  │
│  │  │   /*      ──────────────→ web-dashboard:3000                │  │
│  │  └──────────────────────┘                                      │  │
│  │           │                         │                          │  │
│  │           ▼                         ▼                          │  │
│  │  ┌──────────────────┐    ┌──────────────────────┐              │  │
│  │  │  trading-api      │    │  web-dashboard        │             │  │
│  │  │  Spring Boot 3.2  │    │  Next.js              │             │  │
│  │  │  Java 17          │    │  :3000 (內部)         │             │  │
│  │  │  :8080 (內部)     │    │                        │             │  │
│  │  │                   │    │  NEXT_PUBLIC_API_URL   │             │  │
│  │  │  env: .env.prod   │    │  = trading-api:8080    │             │  │
│  │  └──┬─────────────┘    └────────────────────────┘              │  │
│  │     │  AMQP (:5672)                                            │  │
│  │     ▼                                                          │  │
│  │  ┌──────────────────────┐                                      │  │
│  │  │  RabbitMQ             │  ← :5672  (AMQP, 內部)              │  │
│  │  │  rabbitmq:3-mgmt     │  ← :15672 (Management UI, 內部)     │  │
│  │  │                       │                                     │  │
│  │  │  Exchange: notification.exchange (Direct)                   │  │
│  │  │  Queues:  notification.user / notification.admin            │  │
│  │  │  DLQ:     notification.dlq                                  │  │
│  │  │  Volume:  rabbitmq_data (持久化)                             │  │
│  │  └──────────────────────┘                                      │  │
│  │           │                                                     │  │
│  └───────────┼─────────────────────────────────────────────────────┘  │
│              │                                                        │
└──────────────┼────────────────────────────────────────────────────────┘
               │ JDBC + SSL (port 5432)
               ▼
┌─────────────────────────────────────┐
│      Neon PostgreSQL (雲端)          │
│      Singapore Region               │
│                                      │
│  Host: ep-young-violet-a19s692f      │
│        -pooler.ap-southeast-1        │
│        .aws.neon.tech                │
│  DB:   trading                       │
│  Pool: HikariCP (max 20)            │
└─────────────────────────────────────┘
```

## 本地開發環境

```
┌─────────────────────────────────────┐
│      Python Discord Monitor          │
│      (Mac 本地運行)                   │
│                                      │
│  監聽 Discord 訊號                    │
│         │                            │
│         │ HTTPS                      │
│         ▼                            │
│  POST https://hook-fi.com/api/...   │
│  Header: X-Api-Key: MONITOR_API_KEY │
└─────────────────────────────────────┘
```

## 外部服務

```
┌────────────────┐  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐
│  Binance API    │  │  Discord       │  │  LINE           │  │  Google Gemini  │
│                 │  │  Webhooks      │  │  Messaging API  │  │  AI API         │
│  REST:          │  │                │  │                 │  │                 │
│  fapi.binance   │  │  通知推送       │  │  通知推送        │  │  交易顧問分析    │
│  .com           │  │                │  │  Rich Menu      │  │                 │
│                 │  │                │  │  帳號綁定        │  │                 │
│  WebSocket:     │  │                │  │                 │  │                 │
│  fstream.       │  │                │  │                 │  │                 │
│  binance.com    │  │                │  │                 │  │                 │
└────────────────┘  └────────────────┘  └────────────────┘  └────────────────┘
```

## Port 總覽

### 對外 (Droplet → Internet)
| Port | 服務 | 說明 |
|------|------|------|
| 80/tcp | Caddy | Cloudflare Flexible 模式連入 |
| 443/tcp | Caddy | 保留（目前未使用） |

### Docker 內部 (容器互通)
| Port | 服務 | 說明 |
|------|------|------|
| 8080 | trading-api | Spring Boot API |
| 3000 | web-dashboard | Next.js 前端 |
| 5672 | RabbitMQ | AMQP 訊息佇列 |
| 15672 | RabbitMQ | Management UI（僅內部） |
| 2019 | Caddy admin | 僅 localhost |

### 外連 (Droplet → 外部)
| Port | 目標 | 說明 |
|------|------|------|
| 5432 | Neon PostgreSQL | JDBC + SSL |
| 443 | Binance REST API | 下單、查詢 |
| 443 | Binance WebSocket | 即時倉位監控 |
| 443 | Discord Webhook | 交易通知推送 |
| 443 | LINE Messaging API | LINE 通知 + Rich Menu |
| 443 | Google Gemini API | AI 顧問分析 |
| 443 | Resend Email API | Email OTP 驗證 |

## 排程任務 (Cron)
| 時間 | 功能 |
|------|------|
| 每天 07:55 | 日報預檢 |
| 每天 08:00 | 每日 PnL 報告 → Discord |
| 每天 03:00 | 清理過期 Email OTP |
| 每 30 分鐘 | Binance WebSocket keepalive |
| 每 60 秒 | Monitor 心跳檢查 |
| 每 4 小時 | Gemini AI 顧問分析 |
| 每 5 分鐘 | Rate Limit 清理 |

## 部署指令
```bash
# 更新部署
cd /root/crypto-signal-trader
git pull
docker compose -f docker-compose.cloud.yml up -d --build

# 查看狀態
docker compose -f docker-compose.cloud.yml ps

# 查看 log
docker compose -f docker-compose.cloud.yml logs -f trading-api

# RabbitMQ Management UI（透過 SSH tunnel）
ssh -L 15672:localhost:15672 root@159.223.85.29
# 然後瀏覽 http://localhost:15672 (guest/guest)
```

## 安全提醒
- RabbitMQ 的 15672 (Management UI) 和 5672 (AMQP) 只在 Docker 內部網路暴露
- 如需遠端查看 RabbitMQ UI，請用 SSH tunnel，不要直接開放 port
- 正式環境請修改 `RABBITMQ_USER` / `RABBITMQ_PASS`，不要用 guest/guest
